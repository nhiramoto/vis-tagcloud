var cloudWidth = 500,
    cloudHeight = 200;

var container = d3.select('.right-pane')
                .append('svg')
                    .attr('width', width)
                    .attr('height', height)
                .append('g')
                    .attr('transform', 'translate(50, 50)');

var initCloud = function(data) {
    
};
